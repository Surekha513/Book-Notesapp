<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>📚 My Book Notes</h1>

    <form action="/books" method="POST">
        <input type="text" name="title" placeholder="Book Title" required>
        <input type="text" name="author" placeholder="Author" required>
        <input type="number" name="rating" placeholder="Rating (1-5)" required>
        <input type="text" name="review" placeholder="Review">
        <input type="date" name="date_read" required>
        <input type="text" name="isbn" placeholder="ISBN (for cover)" required>
        <button type="submit">Add Book</button>
    </form>

    <ul>
        <% books.forEach(book => { %>
            <li>
                <img src="<%= book.cover_url %>" alt="Book Cover">
                <h2><%= book.title %> by <%= book.author %></h2>
                <p>⭐ Rating: <%= book.rating %></p>
                <p><%= book.review %></p>
                <p>📅 Read on: <%= book.date_read.toISOString().split('T')[0] %></p>
                <form action="/books/delete/<%= book._id %>" method="POST">
                    <button type="submit">❌ Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>
